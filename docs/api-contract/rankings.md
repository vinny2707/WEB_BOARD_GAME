# Rankings APIs

> Phần này sẽ được bổ sung khi phát triển các endpoint liên quan đến xếp hạng.

## 1. Bảng xếp hạng

**Endpoint:** `GET /api/rankings` (Chưa triển khai)

## 2. Xếp hạng theo game

**Endpoint:** `GET /api/rankings/:gameId` (Chưa triển khai)
